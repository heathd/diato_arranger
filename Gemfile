source 'https://rubygems.org'

ruby File.read(File.dirname(__FILE__) + "/.ruby-version")

gem 'sinatra'
gem 'puma'
gem 'treetop'
gem 'rack'

group :development, :test do
  gem 'rspec'
  gem 'pry'
  # gem 'debugger'
end
